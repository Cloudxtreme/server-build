# Openleaf custom changes

# Linux environment variables
export DISTNAME="$(lsb_release --short --description)"
export DISTRELASE="$(lsb_release --short --release)"
export DISTCODENAME="$(lsb_release --short --codename)"